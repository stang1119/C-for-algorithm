# C program for FGK algorithm 
# dynamic Huffman coding

The input of encoding program is a character string of length less than 200. 

The input of decoding program is a binary string ended with a character '#'
